_schema-version: 3.3.0
ID: inviona-project1
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: inviona-project1-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: inviona-project1-auth
      - name: inviona-project1-db
      - name: inviona-project1-destination

  - name: inviona-project1-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: inviona-project1-db

  - name: inviona-project1-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: invionaproject1doctors
          artifacts:
            - doctors.zip
          target-path: app/
        - name: invionaproject1patients
          artifacts:
            - patients.zip
          target-path: app/
        - name: invionaproject1visits
          artifacts:
            - visits.zip
          target-path: app/
    requires:
      - name: inviona-project1-auth
      - name: srv-api
      - name: inviona-project1-html5-repo-host
        parameters:
          content-target: true

  - name: invionaproject1doctors
    type: html5
    path: app/doctors
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: invionaproject1patients
    type: html5
    path: app/patients
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: invionaproject1visits
    type: html5
    path: app/visits
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: inviona-project1-destinations
    type: com.sap.application.content
    requires:
      - name: inviona-project1-auth
        parameters:
          service-key:
            name: inviona-project1-auth-key
      - name: inviona-project1-html5-repo-host
        parameters:
          service-key:
            name: inviona-project1-html5-repo-host-key
      - name: srv-api
      - name: inviona-project1-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: inviona-project1-html5-repository
              ServiceInstanceName: inviona-project1-html5-repo-host
              ServiceKeyName: inviona-project1-html5-repo-host-key
              sap.cloud.service: invionaproject1.service
            - Name: inviona-project1-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: inviona-project1-auth
              ServiceKeyName: inviona-project1-auth-key
              sap.cloud.service: invionaproject1.service

resources:
  - name: inviona-project1-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: inviona-project1-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'viewer (inviona-project1 ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.viewer'
          - name: 'admin (inviona-project1 ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: inviona-project1-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: inviona-project1-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api

  - name: inviona-project1-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host